[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /product/5) work on direct load/refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
